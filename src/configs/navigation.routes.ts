export const authRoutes = {
  LOG_IN: 'LogIn',
  SIGN_UP: 'SignUp',
  FORGOT_PASSWORD: 'ForgotPassword',
} as const;

export const appRoutes = {
  AUTH_STACK: 'AuthStack',
  HOME: 'Home',
} as const;
